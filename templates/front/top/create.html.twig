{% extends 'front/front.html.twig' %}

{% block title %}Ajouter un Top 5{% endblock %}

{% block body %}
<section class="theCreator">
    <div class="container">
        <div class="form-wrapper">
            <h1 class="title-creator">Créer un nouveau TOP 5</h1>
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.category) }}
    {{ form_row(form.icon) }}
    {{ form_rest(form) }}
    {{ form_end(form) }}
    <a href="{{ path('front_tops_index') }}" class="links">Retour aux tops</a>
    </div>
    </div>
</section>
{% endblock %}
